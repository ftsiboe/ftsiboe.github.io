# Local preview Gemfile. Matches GitHub Pages' classic build (the github-pages
# gem pins Jekyll + all allowed plugins to the exact versions GitHub uses).
# Do NOT also list `gem "jekyll"` separately — it conflicts with github-pages.
#
#   cd docs
#   bundle install
#   bundle exec jekyll serve --baseurl ""   # then open http://localhost:4000
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"   # needed for local serve on Ruby 3+
